export const HTTP_BACKEND = "http://localhost:8080/api/v1";
export const HTTP_BACKEND_CLIENT = "http://<ip>/api/v1"; // -> change to localhost for running loaclly
export const WS_URL = "ws://localhost:8081" // will not use in production add this to run loaclly with port below that is without port handled by nginx
export const WS_URL_CLIENT = "ws://<ip>/ws"

